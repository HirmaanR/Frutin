@model IEnumerable<DataLayer.BlogTag>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Blog.Tittle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tag.Tittle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Blog.Tittle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tag.Tittle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BlogTagID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BlogTagID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BlogTagID })
        </td>
    </tr>
}

</table>
